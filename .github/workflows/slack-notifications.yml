name: Slack Notifications

on:
  push:
    branches:
      - main
      - master
      - develop
      - tri
      - duy
      - bop
      - tuan
  pull_request:
    types: [opened, closed]

jobs:
  notify-slack:
    name: Gửi thông báo đến Slack
    runs-on: ubuntu-latest
    steps:
      # --- THÔNG BÁO MERGE VÀO MAIN ---
      - name: Gửi thông báo Merge vào Main
        if: |
          github.event_name == 'push' &&
          github.ref_name == 'main' &&
          contains(github.event.head_commit.message, 'Merge pull request')
        uses: slackapi/slack-github-action@v1.26.0
        with:
          payload: |
            {
              "text": "✅ Đã gộp code vào MAIN bởi ${{ github.actor }}",
              "attachments": [
                {
                  "color": "#8A2BE2",
                  "author_name": "${{ github.actor }}",
                  "author_link": "http://github.com/${{ github.actor }}",
                  "author_icon": "http://github.com/${{ github.actor }}.png",
                  "title": "Nội dung: ${{ github.event.head_commit.message }}",
                  "title_link": "${{ github.event.compare }}",
                  "text": "Đã gộp code vào nhánh *main*. Nhấn vào link trên để xem các thay đổi."
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

      # --- THÔNG BÁO PUSH LÊN CÁC NHÁNH PHỤ ---
      - name: Gửi thông báo Push (Nhánh phụ)
        if: |
          github.event_name == 'push' &&
          github.ref_name != 'main'
        uses: slackapi/slack-github-action@v1.26.0
        with:
          payload: |
            {
              "text": "🚀 Code mới trên nhánh ${{ github.ref_name }} bởi ${{ github.actor }}",
              "attachments": [
                {
                  "color": "#24a0ed",
                  "author_name": "${{ github.actor }}",
                  "author_link": "http://github.com/${{ github.actor }}",
                  "author_icon": "http://github.com/${{ github.actor }}.png",
                  "title": "Commit: ${{ github.event.head_commit.message }}",
                  "title_link": "${{ github.event.compare }}",
                  "text": "Vừa đẩy code lên nhánh *`${{ github.ref_name }}`*. Nhấn vào link trên để xem chi tiết."
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

      # --- THÔNG BÁO MỞ PULL REQUEST ---
      - name: Gửi thông báo Pull Request đã mở
        if: github.event_name == 'pull_request' && github.event.action == 'opened'
        uses: slackapi/slack-github-action@v1.26.0
        with:
          payload: |
            {
              "text": "📥 Yêu cầu gộp code mới từ ${{ github.actor }}: ${{ github.event.pull_request.title }}",
              "attachments": [
                {
                  "color": "#f2c744",
                  "author_name": "${{ github.actor }}",
                  "author_link": "http://github.com/${{ github.actor }}",
                  "author_icon": "http://github.com/${{ github.actor }}.png",
                  "title": "PR: ${{ github.event.pull_request.title }}",
                  "title_link": "${{ github.event.pull_request.html_url }}",
                  "text": "Yêu cầu gộp nhánh `${{ github.event.pull_request.head.ref }}` vào `${{ github.event.pull_request.base.ref }}`"
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

      # --- THÔNG BÁO ĐÓNG PULL REQUEST (KHÔNG MERGE) ---
      - name: Gửi thông báo Pull Request đã bị Đóng
        if: github.event_name == 'pull_request' && github.event.action == 'closed' && github.event.pull_request.merged == false
        uses: slackapi/slack-github-action@v1.26.0
        with:
          payload: |
            {
              "text": "❌ Đã đóng yêu cầu gộp code: ${{ github.event.pull_request.title }}",
              "attachments": [
                {
                  "color": "#d12f2f",
                  "author_name": "${{ github.actor }}",
                  "author_link": "http://github.com/${{ github.actor }}",
                  "author_icon": "http://github.com/${{ github.actor }}.png",
                  "title": "PR Đã Đóng: ${{ github.event.pull_request.title }}",
                  "title_link": "${{ github.event.pull_request.html_url }}"
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}