name: Slack Notifications

on:
  push:
    branches:
      - main
      - master
      - develop
      - tri
      - duy
      - bop
      - tuan
  pull_request:
    types: [opened, closed] # Bỏ 'reopened' nếu không cần thiết để gọn hơn

jobs:
  notify-slack:
    name: Gửi thông báo đến Slack
    runs-on: ubuntu-latest
    steps:
      # --- THÔNG BÁO MERGE VÀO MAIN (QUAN TRỌNG NHẤT) ---
      - name: Gửi thông báo Merge vào Main
        if: |
          github.event_name == 'push' &&
          github.ref_name == 'main' &&
          contains(github.event.head_commit.message, 'Merge pull request')
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
        with:
          slack_message: |
            *Người thực hiện:* `${{ github.actor }}`
            *Sự kiện:* Gộp code vào nhánh *`main`*
            *Nội dung:* `${{ github.event.head_commit.message }}`
            <${{ github.event.compare }}|Xem các thay đổi>
          slack_title: "✅ Đã gộp code vào MAIN"
          slack_color: '#8A2BE2' # Màu tím cho sự kiện quan trọng
          slack_footer: ''

      # --- THÔNG BÁO PUSH LÊN CÁC NHÁNH PHỤ ---
      - name: Gửi thông báo Push (Nhánh phụ)
        if: |
          github.event_name == 'push' &&
          github.ref_name != 'main'
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
        with:
          slack_message: |
            *Người thực hiện:* `${{ github.actor }}`
            *Sự kiện:* Đẩy code lên nhánh *`${{ github.ref_name }}`*
            *Nội dung commit:* `${{ github.event.head_commit.message }}`
            <${{ github.event.compare }}|Xem các thay đổi>
          slack_title: "🚀 Đã có code mới"
          slack_color: '#24a0ed'
          slack_footer: ''

      # --- THÔNG BÁO MỞ PULL REQUEST ---
      - name: Gửi thông báo Pull Request đã mở
        if: github.event_name == 'pull_request' && github.event.action == 'opened'
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
        with:
          slack_message: |
            *Người tạo:* `${{ github.actor }}`
            *Yêu cầu gộp nhánh:* `${{ github.event.pull_request.head.ref }}` → `${{ github.event.pull_request.base.ref }}`
            *Nội dung:* <${{ github.event.pull_request.html_url }}|${{ github.event.pull_request.title }}>
          slack_title: "📥 Yêu cầu gộp code mới"
          slack_color: '#f2c744'
          slack_footer: ''

      # --- THÔNG BÁO ĐÓNG PULL REQUEST (KHÔNG MERGE) ---
      - name: Gửi thông báo Pull Request đã bị Đóng
        if: github.event_name == 'pull_request' && github.event.action == 'closed' && github.event.pull_request.merged == false
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
        with:
          slack_message: |
            *Người thực hiện:* `${{ github.actor }}`
            *Nội dung:* <${{ github.event.pull_request.html_url }}|${{ github.event.pull_request.title }}>
          slack_title: "❌ Đã đóng yêu cầu gộp code (Không Merge)"
          slack_color: '#d12f2f'
          slack_footer: ''